#!/usr/bin/env bash

# neovim
alias nv=nvim

# color ls
alias ls='ls --color=auto'
alias l='ls'

# clear
alias c='clear'

# git
alias gs="git status"
alias ga="git add"
alias gc="git commit -m"
alias gca="git commit --amend"
alias gp="git push"
alias gpl="git pull"
alias gch="git checkout"
alias gd="git diff"
alias gm="git merge"
alias gl="git log"

# pass
alias pw="pass -c"
